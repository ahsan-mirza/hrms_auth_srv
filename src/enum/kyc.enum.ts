export enum KYC_TYPE {
  AUTOMATIC = 'automatic',
  MANUAL = 'manual',
}

export enum KYC_DOCUMENT {
  PASSPORT = 'passport',
  ID_CARD = 'id_card',
  RESIDENCE_PERMIT = 'residence_permit',
  DRIVER_LICENSE = 'driver_license',
}

export enum FACE_DOCUMENT {
  selfie = 'selfie',
  Liveness_Photo = 'liveness_photo',
}
